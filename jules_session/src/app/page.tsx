import { NexusExperience } from "@/sections/NexusExperience";
import { PortfolioScroll } from "@/sections/PortfolioScroll";

export default function HomePage() {
  return (
    <main>
      <NexusExperience />
      <PortfolioScroll />
    </main>
  );
}
