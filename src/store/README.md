# Store Module (`src/store/`)

## Purpose

Manages global state using Zustand. It acts as the central state synchronization layer between WebGL rendering frames and DOM interface overlays.

## Responsibilities

- **useStore.ts:** Declares states for active districts, guide animation tags, loading values, and active project overlays.
