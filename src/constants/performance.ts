/**
 * PROJECT NEXUS // PERFORMANCE CONSTANTS
 * Responsibility: Stores performance thresholds and target framerates.
 */

export const TARGET_FRAMETIME_MS = 16.67; // target 60fps frame budget
export const MAX_MEMORY_HEAP_MB = 150; // warning threshold for WebGL VRAM usage
export const LOW_PERFORMANCE_FPS_THRESHOLD = 30; // drop quality if FPS dips below 30
export const ASSET_BUDGET_SIZE_LIMIT_MB = 25; // strict budget limits for web models
