/**
 * PROJECT NEXUS // WORLD CONSTANTS
 * Responsibility: Stores layout bounds, coordinates, and physical parameters.
 */

export const TREE_OF_CURIOSITY_COORDINATES: [number, number, number] = [0, 0, 0];
export const DISTRICT_RADIUS = 35.0; // The radius of the contiguous ring structure

export const BOARD_FORM_CONCRETE_OFFSET = 0.05; // 5 centimeters slide for slate nodes
export const EXPOSED_CONDUIT_THICKNESS_BASE = 0.002; // Thin copper wires in early sectors
export const EXPOSED_CONDUIT_THICKNESS_ACCUMULATED = 0.05; // Converged busways at the Terrace
export const METRIC_STEELE_HEIGHT = 4.2; // Height of titanium columns in meters
export const GOLDEN_RATIO_FACTOR = 1.61803398875;
