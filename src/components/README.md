# Components Module (`src/components/`)

## Purpose

Houses all React elements, DOM page layouts, overlays, and canvas viewport mount points.

## Responsibilities

- **canvas/:** Viewport container managing Canvas sizing.
- **dom/:** Global Client Providers configuring motion constraints.
